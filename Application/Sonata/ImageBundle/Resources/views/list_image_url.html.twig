{% extends get_admin_template('base_list_field', admin.code) %}

{% block field%}
    {%- if value is empty -%}
    {%- else -%}
        <img src="{{- value -}}" height="50" width="50">
    {%- endif -%}
{% endblock %}